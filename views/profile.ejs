<!DOCTYPE html>
<html lang="sv">

<%- include('./partials/head') %>

<body class="bg-body">
    <main class="grid-20-1fr min-h-full w-full bg-primary absolute">
        <%- include('./partials/sidebar', { active: '/login' }) %>
        <section class="flex flex-col justify-between bg-secondary rounded-md w-1/2 h-1/2 m-auto">
            <h1 class="text-white text-3xl my-4 text-center">Profil</h1>
            <div class="flex justify-around h-full">
                <div class="text-left px-10">
                    <p class="text-white text-lg font-semibold">Namn: <%= user.name || 'Ej angivet' %></p>
                    <p class="text-white text-lg font-semibold">Efternamn: <%= user.lastname || 'Ej angivet' %></p>
                    <p class="text-white text-lg font-semibold">E-mail: <%= user.email %></p>
                    <p class="text-white text-lg font-semibold">Address: <%= user.address || 'Ej angivet' %></p>
                    <p class="text-white text-lg font-semibold">Telefonnummer: <%= user.phonenumber || 'Ej angivet' %></p>
                </div>
                <div class="flex flex-col">
                    <button class="text-white bg-primary font-bold rounded-md py-2 px-4 mb-6 hover:opacity-80">Mina biljetter</button>
                    <button class="text-white bg-primary font-bold rounded-md py-2 px-4 hover:opacity-80">Tidigare köp</button>
                </div>
            </div>
            <div class="flex justify-around w-full self-end">
                <button class="text-white bg-primary font-bold rounded-md py-2 px-4 my-6 hover:opacity-80">Ändra uppgifter</button>
                <a class="signout text-white bg-primary font-bold rounded-md py-2 px-4 my-6 hover:opacity-80" href="/logout">Logga ut</a>
            </div>
    </main>
</body>
</html>